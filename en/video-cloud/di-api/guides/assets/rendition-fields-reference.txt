
    VideoCodec videoCodec e.g. h264, vp6


    speed Integer 1-5


    Integer videoBitrate in kbps, 123 = 123kbps or 123,000bps


    Integer maxVideoBitrate in kbps, 123 = 123kbps or 123,000bps - h264 only

    Integer decoderBitrateCap    in kbps, the max bitrate fed to the decoder via a buffer


    Integer decoderBufferSize    in kbps, the size of the buffer fed to the decoder when using a bitrate_cap


    Float frameRate    default is "same as source"


    Float keyframeRate    max keyframes per second


    Float maxFrameRate    limits the frameRate rather than sets it, use as an alternative to frameRate


    Integer keyframeInterval    max number of frames between keyframes, default is 250, overrides keyframeRate


    Boolean fixedKeyframeInterval    prevent things like scenecut from placing additional keyframes


    Integer forcedKeyframeInterval    forces a keyframe every X frames, but still allows additional keyframes for scenecut, etc


    Float forcedKeyframeRate    force the keyframe rate, h264 only, ignored if forced_keyframe_interval is used


    Boolean onePass    force one pass, default is false


    Integer width    width in pixels


    height in pixels
    Integer height

    upscale the frame size if the source is smaller than the target, default is false
    Boolean upscale

    preserve, stretch, crop, pad, default is 'preserve'
    AspectMode aspectMode

    number of reference frames to use, default is 3
    Integer h264ReferenceFrames

    e.g. baseline
    H264Profile h264Profile

    h264 profile level, default is calculated
    Float h264Level

    number of bframes, default is 0
    Integer h264Bframes

     film, animation, grain, psnr, ssim, fastdecode, zerolatency
    H264Tuning tuning

    1-51, not used by default. Overrides 'quality'.
    Integer crf

    e.g. baseline
    VP6Profile vp6Profile

    good or best, default is good (balances speed vs quality)
    VP6CompressionMode vp6CompressionMode

    for 2-pass VBR, lowest kbps allowed
    Integer vp62PassMinSection

    for 2-pass VBR, max kbps allowed
    Integer vp62PassMaxSection

    seconds of preload that are necessary before starting playback, default is 6, CBR only
    Integer vp6StreamPrebuffer

    the maximum size of the buffer, in seconds. CBR only.
    Integer vp6StreamMaxBuffer

    adaptive, blur, or drop - default is adaptive
    VP6DeinterlaceMode vp6DeinterlaceMode

    0 to 1.0, use 0 to let Flix estimate noise and vary frame-by-frame, default is 0
    Float vp6DenoiseLevel

    force CBR, requires 'videoBitrate' to be set
    Boolean constantBitrate

    //list of watermarks (burn-in images) to apply to all frames
    Set<Watermark> watermarks

    //
    Boolean copyVideo

    //Streaming Delivery Format typically apply to MPEG-DASH outputs within Zencoder
    String streamingDeliveryFormat

    //Streaming Delivery Profiles typically apply to MPEG-DASH outputs within Zencoder
    String streamingDeliveryProfile