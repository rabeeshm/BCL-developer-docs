#1.1.0
  New:
  - setRegion() and getRegion() methods were added to BCPlayerView
  - Log messages are now turned off by default. To turn them on, a new API
    method logEnabled(true) has been added to BCPlayerView
  Fixes:
  -  BC Player API layouts can now be used on combination with other  	layouts. Exception were being thrown if other layouts were listed before BC Player API layouts.
    
#1.0.0

 New:
  - Initial sdk release.