{
  "title": "Get a copy of (rather than a reference to) an object",
  "cells": [
    {
      "type": "code",
      "language": "sh",
      "data": "/**\n * get a copy of (rather than reference to) an object\n * @param  {object} obj - the object you want a copy\n * @return {object}     the copy\n */\ncopyObj = function (obj) {\n    return JSON.parse(JSON.stringify(obj));\n};\n\n\n\n\n/* don’t know if this is the most efficient way, but certainly a clever one */"
    }
  ]
}