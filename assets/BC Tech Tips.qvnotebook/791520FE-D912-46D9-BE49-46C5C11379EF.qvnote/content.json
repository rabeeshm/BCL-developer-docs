{
  "title": "dedupe simple array (numeric or text items)",
  "cells": [
    {
      "type": "code",
      "language": "sh",
      "data": "/**\n * dedupe a simple array of strings or numbers\n * @param {array} arr the array to be deduped\n * @return {array} out the deduped array\n */\ndedupe = function (arr) {\n    var i,\n        len = arr.length,\n        out = [],\n        obj = {};\n\n    for (i = 0;i < len; i++) {\n        obj[arr[i]] = 0;\n    }\n    for (i in obj) {\n        out.push(i);\n    }\n    return out;\n};"
    }
  ]
}