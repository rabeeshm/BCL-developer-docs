{
  "title": "is integer?",
  "cells": [
    {
      "type": "code",
      "language": "sh",
      "data": "/* function to determine whether a value passed in is an integer */\nfunction isInteger(x) {\n    return Math.round(x) === x;\n}"
    }
  ]
}