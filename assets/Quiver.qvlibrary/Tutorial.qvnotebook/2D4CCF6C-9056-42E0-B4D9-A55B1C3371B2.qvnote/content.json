{
  "title": "isValidJson()",
  "cells": [
    {
      "type": "code",
      "language": "sh",
      "data": "/**\n * check string to insure it's valid json\n * @param {String} json the json to check\n * @return {Boolean} \n */\nfunction isValidJson(json) {\n    try {\n        JSON.parse(json);\n        return true;\n    } catch (e) {\n        return false;\n    }\n}"
    }
  ]
}