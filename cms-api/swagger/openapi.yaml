openapi: 3.0.0
info:
  title: Brightcove CMS API Reference
  description: >-
    Reference for the Brightcove CMS API, used to access and manage your media library. To test API requests, you can use our [API Testing Tools](https://support.brightcove.com/developer-tools-and-concepts). For permissions needed for OAuth credentials, see [API Operations](https://support.brightcove.com/api-operations-client-credentials-requests). For information on authenticating API requests, see the [OAuth Overview](https://support.brightcove.com/overview-oauth-api-v4).

     **Base URL**: https://cms.api.brightcove.com/v1
  version: 1.0.0
servers:
  - url: 'https://cms.api.brightcove.com/v1'
    variables: {}
tags:
  - name: Videos
    description: 'Operations for managing videos, video metadata, audio tracks, and more.'
  - name: Media Sharing
    description: >-
      Operations for setting up sharing relationships and sharing videos between
      different Video Cloud accounts.
  - name: Playlists
    description: Operations for managing video playlists.
  - name: Folders
    description: Operations for managing folders to organize your videos.
  - name: Notifications
    description: >-
      Operations for setting up and managing notifications of changes to your
      video library.
  - name: Media Assets
    description: >-
      Operations for managing renditions, manifests, and other media assets.
      These operations are used mainly for remote assets.
  - name: Legacy
    description: >-
      Legacy operations - unless your account is still using the legacy ingest
      system instead of Dynamic Delivery, these operations are not relevant.
paths:
  '/accounts/{account_id}/videos':
    get:
      tags:
        - Videos
      summary: Get Videos
      description: 'Gets a page of video objects '
      operationId: GetVideos
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
        - $ref: '#/components/parameters/VideoSort'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Video'
        '400':
          description: |-
            INVALID_SORT: sort parameter specified and invalid field
            ILLEGAL_QUERY: The search string syntax was invalid - example
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Videos
      summary: Create Video
      description: >-
        Create a new video object in the account. Note: this does not ingest a
        video file - use the Dynamic Ingest API for ingestion
      operationId: CreateVideo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Create a new video object in the account. Note: this does not ingest a
          video file - use the Dynamic Ingest API for ingestion
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateVideoRequestBodyFields'
        required: true
      responses:
        '201':
          description: Video created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Video'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: 'BAD_VALUE: Spelling error or other use of non-existent field '
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: >-
            REFERENCE_ID_IN_USE: You attempted to create a video with a
            reference id that is already in use, or add a reference id to a
            video which is already used by another video
        '422':
          description: 'ILLEGAL_FIELD: Spelling error or other use of non-existent field '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/create
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/counts/videos':
    get:
      tags:
        - Videos
      summary: Get Video Count
      description: 'Gets count of videos for the account or a search '
      operationId: GetVideoCount
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
        - $ref: '#/components/parameters/VideoSort'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoCount'
        '400':
          description: 'INVALID_SORT: sort parameter specified and invalid field '
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}':
    get:
      tags:
        - Videos
      summary: 'Get Videos by ID/Reference ID'
      description: >-
        Gets a video object - you can include **up to 10** video ids seperated by commas
      operationId: GetVideoByIdOrReferenceId
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Video'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Videos
      summary: Delete Video
      description: 'Deletes a video '
      operationId: DeleteVideo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: ''
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '409':
          description: 'REFERENCES_EXIST: The video is in one or more manual playlists '
        '412':
          description: >-
            PRE_CONDITION_FAILED: usually this means the caller provided an ETag
            that didn&#39;t match the version of the video
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: 'INTERNAL_ERROR: Error in the backend '
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Videos
      summary: Update Video
      description: >-
        Update video metadata - note that this API does not ingest any
        media files - use the Dynamic Ingest API for ingestion. Also note that replacing WebVTT text tracks is a two-step operation - see Add WebVTT Captions for details.
      operationId: UpdateVideo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Update video metadata - note that this API does not ingest any media files - use the Dynamic Ingest API for ingestion. Also note that replacing WebVTT text tracks is a two-step operation - see Add WebVTT Captions for details.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Video'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Video'
        '400':
          description: >-
            AD_CONFIG_INACTIVE: Ad configuration specified in an SSAI request is inactive

            AD_CONFIG_NOT_FOUND: Ad configuration specified in an SSAI request was not found
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: 'BAD_VALUE: Spelling error or other use of non-existent field '
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: >-
            REFERENCE_ID_IN_USE: You attempted to create a video with a reference id that is already in use, or add a reference id to a video which is already used by another video
        '422':
          description: >-
            ILLEGAL_FIELD: Spelling error or other use of non-existent field

            VALIDATION_ERROR: the JSON data was not valid - error messages vary depending on the problem
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: >-
            UNKNOWN: an unknown internal error occurred - this might be a
            temporary system issue, but if the problem persists, it is likely an uncaught error in the request - contact Support
        '503':
          description: 'TIMEOUT: Server likely too busy - try again later '
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/sources':
    get:
      tags:
        - Videos
      summary: Get Video Sources
      description: 'Gets an array of sources (renditions) for a video'
      operationId: GetVideoSources
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoSources'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/images':
    get:
      tags:
        - Videos
      summary: Get Video Images
      description: 'Gets the images for a video '
      operationId: GetVideoImages
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoImages'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/audio_tracks':
    get:
      tags:
        - Videos
      summary: Get Video Audio Tracks
      description: 'Gets the audio tracks for a video Dynamic Delivery only '
      operationId: GetVideoAudioTracks
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AudioTrack'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/audio_tracks/{audio_track_id}':
    get:
      tags:
        - Videos
      summary: Get Video Audio Track
      description: 'Gets one audio track for a video by its ID Dynamic Delivery only '
      operationId: GetVideoAudioTrack
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AudioTrackId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AudioTrack'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Videos
      summary: Update Video Audio Track
      description: 'Updates audio track metadata for a video Dynamic Delivery only '
      operationId: UpdateVideoAudioTrack
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AudioTrackId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: Updates audio track metadata for a video Dynamic Delivery only
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AudioTrack'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AudioTrack'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Videos
      summary: Delete Video Audio Track
      description: 'Deletes one audio track for a video by its ID Dynamic Delivery only '
      operationId: DeleteVideoAudioTrack
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AudioTrackId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/digital_master':
    get:
      tags:
        - Videos
      summary: Get Digital Master Info
      description: 'Gets the stored digital master for a video, if any '
      operationId: GetDigitalMasterInfo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DigitalMaster'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Videos
      summary: Delete Digital Master
      description: >-
        Deletes the archived digital master for a video. Be sure to read Digital Master Delete API before using this operation to understand the implications.
      operationId: DeleteDigitalMaster
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/asset/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/references':
    get:
      tags:
        - Videos
      summary: Get Playlists for Video
      description: >-
        Gets an array of Manual (EXPLICIT) playlists that contain a video object for the account
      operationId: GetPlaylistsForVideo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistReferences'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Videos
      summary: Remove Video from all Playlists
      description: 'Removes the video from all EXPLICIT playlists for the account '
      operationId: RemoveVideoFromAllPlaylists
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/ingest_jobs':
    get:
      tags:
        - Videos
      summary: Get Status of Ingest Jobs
      description: >-
        Get the status of all ingest jobs associated with a video (including the original ingestion, replacing and retranscoding the video). NOTE: this operation only works for videos that were ingested using **Dynamic Delivery** profiles.
      operationId: GetStatusOfIngestJobs
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/IngestJobStatus'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: 'INTERNAL_ERROR: Error in the backend '
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/ingest_jobs/{job_id}':
    get:
      tags:
        - Videos
      summary: Get Status of Ingest Job
      description: >-
        Get the status of an ingest job associated with a video (including the original ingestion, replacing and retranscoding the video). NOTE: this operation only works for videos that were ingested using **Dynamic Delivery** profiles.
      operationId: GetStatusOfIngestJob
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/JobId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngestJobStatus'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: 'INTERNAL_ERROR: Error in the backend '
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/video_fields':
    get:
      tags:
        - Videos
      summary: Get Custom Fields
      description: 'Gets a list of custom fields for the account '
      operationId: GetCustomFields
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoFields'
        '400':
          description: >-
            REFERENCES_EXIST: You are attempting to delete a video that is
            included in at least one playlist

            SHARED_VIDEO: Deletion of shared videos is not yet supported
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/channels':
    get:
      tags:
        - Media Sharing
      summary: List Channels
      description: >-
        Gets a list of channels (currently there is only one default channel) - this is a Master account operation
      operationId: ListChannels
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Channel'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact Support
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/channels/{channel_name}':
    get:
      tags:
        - Media Sharing
      summary: Get Channel Details
      description: >-
        Gets settings for a sharing channel (currently there is only one default channel) - this is a Master account operation
      operationId: GetChannelDetails
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ChannelName'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Channel'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact Support
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Sharing
      summary: Update Channel
      description: >-
        Updates settings for a sharing channel (currently there is only one default channel) - this is a Master account operation
      operationId: UpdateChannel
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ChannelName'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates settings for a sharing channel (currently there is only one
          default channel) - this is a Master account operation
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Channel'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Channel'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/channels/{channel_name}/members':
    get:
      tags:
        - Media Sharing
      summary: List Channel Affiliates
      description: >-
        Gets a list of affiliates for a channel - this is a Master account
        operation
      operationId: ListChannelAffiliates
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ChannelName'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ChannelAffiliate'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/channels/{channel_name}/members/{affiliate_account_id}':
    put:
      tags:
        - Media Sharing
      summary: Add Affiliate
      description: 'Adds an affiliate to a channel - this is a Master account operation '
      operationId: AddAffiliate
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ChannelName'
        - $ref: '#/components/parameters/AffiliateAccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: Adds an affiliate to a channel - this is a Master account operation
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddAffiliate'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddAffiliate'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Sharing
      summary: Remove Affiliate
      description: >-
        Removes an affiliate from a channel - this is a Master account operation
      operationId: RemoveAffiliate
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ChannelName'
        - $ref: '#/components/parameters/AffiliateAccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/contracts/':
    get:
      tags:
        - Media Sharing
      summary: Approve Contract
      description: >-
        Gets a list of available sharing contracts - this is an Affiliate account operation
      operationId: ApproveContract
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contract'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/contracts/{master_account_id}':
    get:
      tags:
        - Media Sharing
      summary: Approve Contract
      description: >-
        Gets a list of available sharing contracts - this is an Affiliate account operation
      operationId: ApproveContract
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/MasterAccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contract'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Sharing
      summary: Approve Contract
      description: 'Approve a contract - this is an Affiliate account operation '
      operationId: ApproveContract
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/MasterAccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Gets a list of available sharing contracts - this is an Affiliate
          account operation
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Contract'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contract'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/sharing-relationships/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/shares':
    get:
      tags:
        - Media Sharing
      summary: List Shares
      description: >-
        Lists the existing shares for an account - this is a Master account operation - do this before sharing to insure that you are not re-sharing to an affiliate, which would overwrite any affiliate metadata changes
      operationId: ListShares
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VideoShare'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Sharing
      summary: Share Video
      description: >-
        Shares a video to one or more affiliates - this is an Master account operation - if the video has already been shared to an affiliate, this operation will re-share it and overwrite any affiliate metadata changes
      operationId: ShareVideo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Shares a video to one or more affiliates - this is an Master account operation - if the video has already been shared to an affiliate, this operation will re-share it and overwrite any affiliate metadata
          changes
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/ShareVideoRequest'
              description: >-
                Shares a video to one or more affiliates - this is an Master account operation - if the video has already been shared to an affiliate, this operation will re-share it and overwrite any affiliate metadata changes
        required: true
      responses:
        '202':
          description: Share request accepted
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VideoShare'
                description: Share request accepted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/create
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/shares/{affiliate_account_id}':
    get:
      tags:
        - Media Sharing
      summary: Get Share
      description: >-
        Lists the existing shares for an account - this is a Master account operation - do this before sharing to insure that you are not re-sharing to an affiliate, which would overwrite any affiliate metadata changes
      operationId: GetShare
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AffiliateAccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoShare'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Sharing
      summary: Unshare Video
      description: >-
        Unshares a video with a specific affiliate - this is an Master account operation - do this before sharing to insure that you are not re-sharing to an affiliate, which would overwrite any affiliate metadata changes
      operationId: UnshareVideo
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AffiliateAccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is  unavailable - this may be a temporary condition while some kind of processing of the video is in progress, but if the message persists, contact
        '404':
          description: 'RESOURCE_NOT_FOUND: Resource not found '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/playlists':
    get:
      tags:
        - Playlists
      summary: Get Playlists
      description: 'Gets a page of playlist objects for the account '
      operationId: GetPlaylists
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
        - $ref: '#/components/parameters/PlaylistSort'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Playlist'
        '400':
          description: >-
            'INVALID_SORT: sort parameter specified and invalid field

            INVALID_SEARCH: search string invalid (may not have been
            URI-encoded)'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed - check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Playlists
      summary: Create Playlist
      description: 'Creates a new playlist '
      operationId: CreatePlaylist
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: Creates a new playlist
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Playlist'
        required: true
      responses:
        '201':
          description: Playlist created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playlist'
        '400':
          description: 'BAD_VALUE: The JSON could not be parsed '
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'ILLEGAL_FIELD: Spelling error or use of non-existent field '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/create
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/counts/playlists':
    get:
      tags:
        - Playlists
      summary: Get Playlist Count
      description: 'Gets a count of playlists in the account for the account '
      operationId: GetPlaylistCount
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
        - $ref: '#/components/parameters/PlaylistSort'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistCount'
        '400':
          description: >-
            'INVALID_SORT: sort parameter specified and invalid field

            INVALID_SEARCH: search string invalid (may not have been
            URI-encoded)'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed - check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/playlists/{playlist_id}':
    get:
      tags:
        - Playlists
      summary: Get Playlists by ID
      description: 'Gets one or more playlist objects for the account '
      operationId: GetPlaylistById
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - name: playlist_id
          in: path
          description: >-
            Video Cloud playlist ID, or multiple playlist ids separated by commas.
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playlist'
        '400':
          description: >-
            'INVALID_SORT: sort parameter specified and invalid field

            INVALID_SEARCH: search string invalid (may not have been
            URI-encoded)'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed - check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Playlists
      summary: Update Playlist
      description: 'Updates a playlist for the account '
      operationId: UpdatePlaylist
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/PlaylistId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: Updates a playlist for the account
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Playlist'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playlist'
        '400':
          description: 'BAD_VALUE: The JSON could not be parsed '
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'ILLEGAL_FIELD: Spelling error or use of non-existent field '
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Playlists
      summary: Delete Playlist
      description: 'Deletes a playlist '
      operationId: DeletePlaylists
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/PlaylistId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: ''
        '400':
          description: >-
            'INVALID_SORT: sort parameter specified and invalid field

            INVALID_SEARCH: search string invalid (may not have been URI-encoded)'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed - check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/playlists/{playlist_id}/videos':
    get:
      tags:
        - Playlists
      summary: Get Videos in Playlist
      description: 'Gets the video objects for videos in a playlist for the account '
      operationId: GetVideosInPlaylist
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/PlaylistId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Video'
        '400':
          description: >-
            'INVALID_SORT: sort parameter specified and invalid field

            INVALID_SEARCH: search string invalid (may not have been URI-encoded)'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed - check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/counts/playlists/{playlist_id}/videos':
    get:
      tags:
        - Playlists
      summary: Get Video Count in Playlist
      description: 'Gets a count of the videos in a playlist for the account '
      operationId: GetVideoCountInPlaylist
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/PlaylistId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoCountInPlaylist'
        '400':
          description: >-
            'INVALID_SORT: sort parameter specified and invalid field

            INVALID_SEARCH: search string invalid (may not have been
            URI-encoded)'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed - check to make sure your client credentials were correct for the access token
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/playlist/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/folders':
    get:
      tags:
        - Folders
      summary: Get Folders
      description: 'Gets list of folders for the account '
      operationId: GetFolders
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Folders
      summary: Create Folder
      description: 'Create a new folder for the account '
      operationId: CreateFolder
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: Create a new folder for the account
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Folder'
        required: true
      responses:
        '201':
          description: Folder created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/create
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/folders/{folder_id}':
    get:
      tags:
        - Folders
      summary: Get Folder Information
      description: 'Gets information about a folder '
      operationId: GetFolderInformation
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/FolderId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Folder'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Folders
      summary: Update Folder Name
      description: 'Update the folder name '
      operationId: UpdateFolderName
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - name: folder_id
          in: path
          description: the folder ID
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: Update the folder name
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Folder'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Folders
      summary: Delete Folder
      description: 'Delete a folder '
      operationId: DeleteFolder
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - name: folder_id
          in: path
          description: the folder ID
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - name: name
          in: query
          description: 'name of the folder (must be unique in the account)'
          required: true
          style: form
          explode: true
          schema:
            type: string
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '201':
          description: '201'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/delete
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/folders/{folder_id}/videos':
    get:
      tags:
        - Folders
      summary: Get Videos in Folder
      description: 'Gets list of video objects in a folder '
      operationId: GetVideosInFolder
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - name: folder_id
          in: path
          description: the folder ID.
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Videos in the folder
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Video'
                description: Videos in the folder
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/folders/{folder_id}/videos/{video_id}':
    put:
      tags:
        - Folders
      summary: Add Video to Folder
      description: 'Add a video to a folder '
      operationId: AddVideoToFolder
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - name: folder_id
          in: path
          description: the folder ID
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - name: video_id
          in: path
          description: the video ID
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: ''
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Folders
      summary: Remove Video from Folder
      description: 'Remove a video from a folder '
      operationId: RemoveVideoFromFolder
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - name: folder_id
          in: path
          description: the folder ID
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - name: video_id
          in: path
          description: the video ID
          required: true
          style: simple
          explode: false
          schema:
            type: string
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '201':
          description: Video successfully removed from the folder
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/subscriptions':
    get:
      tags:
        - Notifications
      summary: Get Subscriptions List
      description: 'Get a list of all notification subscriptions for the account '
      operationId: GetSubscriptionsList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Subscription'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/notifications/all
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Notifications
      summary: Create Subscription
      description: >-
        Establishes up to 10 endpoints that video changes should be sent to. Any
        change in video metadata will trigger a video change event and a
        notification &mdash; changes to assets used by the video will not
        trigger change events.
      operationId: CreateSubscription
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Establishes up to 10 endpoints that video changes should be sent to.
          Any change in video metadata will trigger a video change event and a
          notification &mdash; changes to assets used by the video will not
          trigger change events.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Subscription'
        required: true
      responses:
        '201':
          description: Subscription created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subscription'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '422':
          description: >-
            Unprocessable_Entity: 1) You already have a subscription for the
            video-change event that delivers to that address; 2) the endpoint or
            events field is missing from the request; 3) you already have 10
            subscriptions to this event
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/notifications/all
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/subscriptions/{subscription_id}':
    get:
      tags:
        - Notifications
      summary: Get Subscription
      description: 'Get a notification subscription for the account '
      operationId: GetSubscription
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/SubscriptionId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subscription'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/notifications/all
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Notifications
      summary: Delete Subscription
      description: 'Delete a notification subscription for the account '
      operationId: DeleteSubscription
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/SubscriptionId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/notifications/all
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets':
    get:
      tags:
        - Media Assets
      summary: Get Assets
      description: >-
        Gets assets for a given video. Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetAssets
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VideoAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/dynamic_renditions':
    get:
      tags:
        - Media Assets
      summary: Get Dynamic Renditions
      description: >-
        Gets a list of dynamic renditions for a Dynamic Delivery video. Note:
        you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetDynamicRenditions
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DynamicRendition'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/renditions':
    get:
      tags:
        - Media Assets
      summary: Get Rendition List
      description: >-
        Gets a list of renditions for a given video. Notes: 1) this endpoint is
        for renditions created using the legacy ingest profiles - for Dynamic
        Deliver renditions, use the
        `/accounts/{account_id}/videos/{video_id}/assets/dynamic_renditions`
        endpoint; 2) you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: GetRenditionList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VideoAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Assets
      summary: Add Rendition
      description: >-
        Add a remote rendition to the given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: AddRendition
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Add a remote rendition to the given video. Note: you can use
          `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VideoAsset'
        required: true
      responses:
        '201':
          description: Rendition asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable

            MIXED_REMOTE_NOT_ALLOWED: Remote renditions may not be added to
            ingested videos
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/renditions/{asset_id}':
    get:
      tags:
        - Media Assets
      summary: Get Rendition
      description: >-
        Gets a specified rendition for a video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetRendition
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Assets
      summary: Update Rendition
      description: >-
        Update the location for a remote rendition. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: UpdateRendition
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Update the location for a remote rendition. Note: you can use
          `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VideoAsset'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable

            MIXED_REMOTE_NOT_ALLOWED: Remote renditions may not be added to
            ingested videos
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Assets
      summary: Delete Rendition
      description: >-
        Deletes a rendition for the given video. Note: this operation is only
        for remote renditions for remote asset videos &mdash; do not use it for
        renditions created by Video Cloud for ingested videos
      operationId: DeleteRendition
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/hls_manifest':
    get:
      tags:
        - Media Assets
      summary: Get HLS Manifest List
      description: >-
        Gets the hls_manifest for a given video. Notes: 1) you can use
        `/videos/ref:reference_id` instead of `/videos/video_id` 2) this method only
        returns a remote asset HTL manifest, not manifests for ingested videos
      operationId: GetHlsManifestList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Assets
      summary: Add HLS Manifest
      description: >-
        Adds the location of an hls_manifest file for a remote asset. Note: you
        can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: AddHlsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds the location of an hls_manifest file for a remote asset. Note:
          you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '201':
          description: Manifest asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/hls_manifest/{asset_id}':
    get:
      tags:
        - Media Assets
      summary: Get HLS Manifest
      description: >-
        Gets an hls_manifest for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetHlsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Assets
      summary: Update HLS Manifest
      description: >-
        Updates the location of a remote hls_manifest file for a remote asset.
        Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: UpdateHlsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote hls_manifest file for a remote asset.
          Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Assets
      summary: Delete HLS Manifest
      description: >-
        Deletes an hls_manifest file for a remote asset. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: DeleteHlsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/dash_manifests':
    get:
      tags:
        - Media Assets
      summary: Get DASH Manifest List
      description: >-
        Gets the dash_manifests for a given video. Notes: 1. you can have
        multiple dash manifests with profiles; you can have only one dash
        manifest without a profile, but one manifest without a profile can be
        combined with multiple manifests with profiles; 2. all manifests
        intended to be used with the CMS API should include a profile - only
        DASH manifests with profiles will be returned by the CMS API &mdash;
        only a single DASH manifest without a profile will be returned by the
        Media API 3. you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: GetDashManifestList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Assets
      summary: Add DASH Manifest
      description: >-
        Adds a location for a remote DASH manifest Notes: 1. you can have
        multiple dash manifests with profiles; you can have only one dash
        manifest without a profile, but one manifest without a profile can be
        combined with multiple manifests with profiles; 2. all manifests
        intended to be used with the CMS API should include a profile - only
        DASH manifests with profiles will be returned by the CMS API &mdash;
        only a single DASH manifest without a profile will be returned by the
        Media API 3. you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: AddDashManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds a location for a remote DASH manifest Notes: 1. you can have
          multiple dash manifests with profiles; you can have only one dash
          manifest without a profile, but one manifest without a profile can be
          combined with multiple manifests with profiles; 2. all manifests
          intended to be used with the CMS API should include a profile - only
          DASH manifests with profiles will be returned by the CMS API &mdash;
          only a single DASH manifest without a profile will be returned by the
          Media API 3. you can use /videos/ref:reference_id instead of
          /videos/video_id
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '201':
          description: Manifest asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/dash_manifests/{asset_id}':
    get:
      tags:
        - Media Assets
      summary: Get DASH Manifest
      description: >-
        Gets a dash_manifest for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetDashManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Assets
      summary: Update DASH Manifest
      description: >-
        Updates the location of a remote dash_manifest file for a remote asset.
        Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: UpdateDashManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote dash_manifest file for a remote
          asset. Note: you can use /videos/ref:reference_id instead of
          /videos/video_id
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Assets
      summary: Delete DASH Manifest
      description: >-
        Deletes an dash_manifest file for a remote asset. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: DeleteDashManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/hds_manifest':
    get:
      tags:
        - Media Assets
      summary: Get HDS Manifest List
      description: >-
        Gets the hds_manifest file for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetHdsManifestList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Assets
      summary: Add HDS Manifest
      description: >-
        Adds the location of an hds_manifest file for a remote asset. Note: you
        can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: AddHdsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds the location of an hds_manifest file for a remote asset. Note:
          you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '201':
          description: Manifest asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/hds_manifest/{asset_id}':
    get:
      tags:
        - Media Assets
      summary: Get HDS Manifest
      description: >-
        Gets the hds_manifest file for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetHdsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Assets
      summary: Update HDS Manifest
      description: >-
        Updates the location of a remote hds_manifest file for a remote asset.
        Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: UpdateHdsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote hds_manifest file for a remote asset.
          Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Assets
      summary: Delete HDS Manifest
      description: >-
        Deletes an hds_manifest file for a remote asset. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: DeleteHdsManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/ism_manifest':
    get:
      tags:
        - Media Assets
      summary: Get ISM Manifest List
      description: >-
        Gets the ism_manifest for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetIsmManifestList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Assets
      summary: Add ISM Manifest
      description: >-
        Adds the location of an ism_manifest file for a remote asset. Note: you
        can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: AddIsmManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds the location of an ism_manifest file for a remote asset. Note:
          you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '201':
          description: Manifest asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/ism_manifest/{asset_id}':
    get:
      tags:
        - Media Assets
      summary: Get ISM Manifest
      description: >-
        Gets an ism_manifest for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetIsmManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Assets
      summary: Update ISM Manifest
      description: >-
        Updates the location of a remote ism_manifest file for a remote asset.
        Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: UpdateIsmManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote ism_manifest file for a remote asset.
          Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Assets
      summary: Delete ISM Manifest
      description: >-
        Deletes an ism_manifest file for a remote asset. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: DeleteIsmManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/ismc_manifest':
    get:
      tags:
        - Media Assets
      summary: Get ISMC Manifest List
      description: >-
        Gets the ismc_manifest files for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetIsmcManifestList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Media Assets
      summary: Add ISMC Manifest
      description: >-
        Adds the location of an ismc_manifest file for a remote asset. Note: you
        can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: AddIsmcManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds the location of an ismc_manifest file for a remote asset. Note:
          you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '201':
          description: Manifest asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/ismc_manifest/{asset_id}':
    get:
      tags:
        - Media Assets
      summary: Get ISMC Manifest
      description: >-
        Gets the ismc_manifest file for a given video. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: GetIsmcManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Media Assets
      summary: Update ISMC Manifest
      description: >-
        Updates the location of a remote ismc_manifest file for a remote asset.
        Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: UpdateIsmcManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote ismc_manifest file for a remote
          asset. Note: you can use /videos/ref:reference_id instead of
          /videos/video_id
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Media Assets
      summary: Delete ISMC Manifest
      description: >-
        Deletes an ismc_manifest file for a remote asset. Note: you can use
        `/videos/ref:reference_id` instead of `/videos/video_id`
      operationId: DeleteIsmcManifest
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/poster':
    get:
      tags:
        - Legacy
      summary: Get Poster List
      description: >-
        Gets the poster file for a given video. Note that you can only add one
        poster for a video. Note: you can use /videos/ref:reference_id instead
        of /videos/video_id. **This request works for accounts on the legacy
        ingest system only - not supported for Dynamic Delivery**
      operationId: GetPosterList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Legacy
      summary: Add Poster
      description: >-
        Adds a poster file for a remote asset. Ingested assets must be added via
        the Dynamic Ingest API. Note: you can use /videos/ref:reference_id
        instead of /videos/video_id. **This request works for accounts on the
        legacy ingest system only - not supported for Dynamic Delivery**
      operationId: AddPoster
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds a poster file for a remote asset. Ingested assets must be added
          via the Dynamic Ingest API. Note: you can use /videos/ref:reference_id
          instead of /videos/video_id
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImageAsset'
        required: true
      responses:
        '201':
          description: Poster asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/poster/{asset_id}':
    get:
      tags:
        - Legacy
      summary: Get Poster
      description: >-
        Gets a poster file for a given video. Note that you can only add one
        poster for a video. Note: you can use /videos/ref:reference_id instead
        of /videos/video_id. **This request works for accounts on the legacy
        ingest system only - not supported for Dynamic Delivery**
      operationId: GetPoster
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Legacy
      summary: Update Poster
      description: >-
        Updates the location of a remote poster file for a remote asset. Note:
        you can use `/videos/ref:reference_id` instead of `/videos/video_id`. **This
        request works for accounts on the legacy ingest system only - not
        supported for Dynamic Delivery**
      operationId: UpdatePoster
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote poster file for a remote asset. Note:
          you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImageAsset'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Legacy
      summary: Delete Poster
      description: >-
        Deletes a poster file for a remote asset. Note that you can only add one
        poster for a video. Note: you can use /videos/ref:reference_id instead
        of /videos/video_id. **This request works for accounts on the legacy
        ingest system only - not supported for Dynamic Delivery**
      operationId: DeletePoster
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/thumbnail':
    get:
      tags:
        - Legacy
      summary: Get Thumbnail List
      description: >-
        Gets the thumbnail for a given video. Note that you can only add one
        thumbnail for a video. Note: you can use `/videos/ref:reference_id`
        instead of `/videos/video_id`. **This request works for accounts on the
        legacy ingest system only - not supported for Dynamic Delivery**
      operationId: GetThumbnailList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Legacy
      summary: Add_Thumbnail
      description: >-
        Adds a thumbnail file for a remote asset. Ingested assets must be added
        via the Dynamic Ingest API. Note: you can use /videos/ref:reference_id
        instead of /videos/video_id. **This request works for accounts on the
        legacy ingest system only - not supported for Dynamic Delivery**
      operationId: AddThumbnail
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds a thumbnail file for a remote asset. Ingested assets must be
          added via the Dynamic Ingest API. Note: you can use
          `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImageAsset'
        required: true
      responses:
        '201':
          description: Thumbnail asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/thumbnail/{asset_id}':
    get:
      tags:
        - Legacy
      summary: Get_Thumbnail
      description: >-
        Gets a thumbnail file for a given video. Note that you can only add one
        thumbnail for a video. Note: you can use /videos/ref:reference_id
        instead of /videos/video_id. **This request works for accounts on the
        legacy ingest system only - not supported for Dynamic Delivery**
      operationId: GetThumbnail
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Legacy
      summary: Update_Thumbnail
      description: >-
        Updates the location of a remote thumbnail file for a remote asset.
        Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`.
        **This request works for accounts on the legacy ingest system only - not
        supported for Dynamic Delivery**
      operationId: UpdateThumbnail
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote thumbnail file for a remote asset.
          Note: you can use `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImageAsset'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAsset'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Legacy
      summary: Delete_Thumbnail
      description: >-
        Deletes a thumbnail file for a remote asset. Note that you can only add
        one thumbnail for a video. Note: you can use /videos/ref:reference_id
        instead of /videos/video_id. **This request works for accounts on the
        legacy ingest system only - not supported for Dynamic Delivery**
      operationId: DeleteThumbnail
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/caption':
    get:
      tags:
        - Legacy
      summary: Get_Caption_List
      description: >-
        Gets the caption file for a given video (DFXP captions for the Smart
        Player). Note: 1) the caption endpoint is ONLY for working with DFXP
        captions used in the legacy Smart Player - WebVTT captions (text_tracks)
        for the new Brightcove Player are managed using the Update Video
        operation; 2) you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: GetCaptionList
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    post:
      tags:
        - Legacy
      summary: Add_Caption
      description: >-
        Adds a caption file for a remote asset (DFXP captions for the Smart
        Player). Note: 1) the caption endpoint is ONLY for working with DFXP
        captions used in the legacy Smart Player - WebVTT captions (text_tracks)
        for the new Brightcove Player are managed using the Update Video
        operation; 2) you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: AddCaption
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Adds a caption file for a remote asset (DFXP captions for the Smart
          Player). Note: 1) the caption endpoint is ONLY for working with DFXP
          captions used in the legacy Smart Player - WebVTT captions
          (text_tracks) for the new Brightcove Player are managed using the
          Update Video operation; 2) you can use /videos/ref:reference_id
          instead of /videos/video_id
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '201':
          description: Caption asset created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
  '/accounts/{account_id}/videos/{video_id}/assets/caption/{asset_id}':
    get:
      tags:
        - Legacy
      summary: Get_Caption
      description: >-
        Gets a caption file for a given video (DFXP captions for the Smart
        Player). Note: 1) the caption endpoint is ONLY for working with DFXP
        captions used in the legacy Smart Player - WebVTT captions (text_tracks)
        for the new Brightcove Player are managed using the Update Video
        operation; 2) you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: GetCaption
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/read
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    patch:
      tags:
        - Legacy
      summary: Update_Caption
      description: >-
        Updates the location of a remote caption file for a remote asset (DFXP
        captions for the Smart Player). Note: 1) the caption endpoint is ONLY
        for working with DFXP captions used in the legacy Smart Player - WebVTT
        captions (text_tracks) for the new Brightcove Player are managed using
        the Update Video operation; 2) you can use /videos/ref:reference_id
        instead of /videos/video_id
      operationId: UpdateCaption
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      requestBody:
        description: >-
          Updates the location of a remote caption file for a remote asset (DFXP
          captions for the Smart Player). Note: 1) the caption endpoint is ONLY
          for working with DFXP captions used in the legacy Smart Player -
          WebVTT captions (text_tracks) for the new Brightcove Player are
          managed using the Update Video operation; 2) you can use
          `/videos/ref:reference_id` instead of `/videos/video_id`
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Manifest'
        required: true
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '409':
          description: 'REFERENCE_ID_IN_USE: The specified reference id is already in use '
        '422':
          description: 'VALIDATION_ERROR: remote_url'
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
    delete:
      tags:
        - Legacy
      summary: Delete_Caption
      description: >-
        Deletes a caption file for a remote asset (DFXP captions for the Smart
        Player). Note: 1) the caption endpoint is ONLY for working with DFXP
        captions used in the legacy Smart Player - WebVTT captions (text_tracks)
        for the new Brightcove Player are managed using the Update Video
        operation; 2) you can use /videos/ref:reference_id instead of
        /videos/video_id
      operationId: DeleteCaption
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/VideoId'
        - $ref: '#/components/parameters/AssetId'
        - $ref: '#/components/parameters/ContentType'
        - $ref: '#/components/parameters/Authorization'
      responses:
        '204':
          description: Item was deleted
        '401':
          description: >-
            UNAUTHORIZED: Authentication failed; check to make sure your client credentials were correct for the access token
        '403':
          description: >-
            NOT_AVAILABLE: The resource you are requesting is temporarily unavailable
        '404':
          description: >-
            RESOURCE_NOT_FOUND: The api could not find the resource you requested
        '405':
          description: >-
            METHOD_NOT_ALLOWED: The HTTP method specified is not allowed for this endpoint
        '429':
          description: >-
            TOO_MANY_REQUESTS: You are submitting too many simultaneous requests or too many requests per second
        '500':
          description: |-
            UNKNOWN: Issue in Brightcove system; try again later.
            TIMEOUT: Server likely too busy; try again later.
      deprecated: false
      security:
        - BC_OAuth2:
            - video-cloud/video/update
      x-operation-settings:
        CollectParameters: false
        AllowDynamicQueryParameters: false
        AllowDynamicFormParameters: false
        IsMultiContentStreaming: false
components:
  parameters:
    ContentType:
      name: Content-Type
      in: header
      description: 'Content-Type: application/json'
      required: true
      style: simple
      explode: false
      schema:
        type: string
        default: application/json
      example: application/json
    Authorization:
      name: Authorization
      in: header
      description: >-
        Authorization: Bearer {access_token} - see [Getting Access
        Tokens](https://support.brightcove.com/oauth-api-sample-get-access-token)
      required: true
      style: simple
      explode: false
      schema:
        type: string
        default: application/json
    AccountId:
      name: account_id
      in: path
      description: Video Cloud account ID.
      required: true
      style: simple
      explode: false
      schema:
        type: string
    VideoId:
      name: video_id
      in: path
      description: Video ID.
      required: true
      style: simple
      explode: false
      schema:
        type: string
    PlaylistId:
      name: playlist_id
      in: path
      description: Playlist ID.
      required: true
      style: simple
      explode: false
      schema:
        type: string
    AudioTrackId:
      name: audio_track_id
      in: path
      description: 'the id for the audio track, formed as &#39;language_variant&#39;'
      required: true
      style: simple
      explode: false
      schema:
        type: string
    SubscriptionId:
      name: subscription_id
      in: path
      description: Subscription ID.
      required: true
      style: simple
      explode: false
      schema:
        type: string
    JobId:
      name: job_id
      in: path
      description: >-
        The Job ID for the ingest job (returned in the response to an ingest
        request
      required: true
      style: simple
      explode: false
      schema:
        type: string
    FolderId:
      name: folder_id
      in: path
      description: Folder ID.
      required: true
      style: simple
      explode: false
      schema:
        type: string
    AssetId:
      name: asset_id
      in: path
      description: The asset ID
      required: true
      style: simple
      explode: false
      schema:
        type: string
    AffiliateAccountId:
      name: affiliate_account_id
      in: path
      description: Video Cloud affiliate account ID for media sharing relationships
      required: true
      style: simple
      explode: false
      schema:
        type: string
    MasterAccountId:
      name: master_account_id
      in: path
      description: Video Cloud master account ID for media sharing relationships
      required: true
      style: simple
      explode: false
      schema:
        type: string
    ChannelName:
      name: channel_name
      in: path
      description: The name of the channel - usually `default`
      required: true
      style: simple
      explode: false
      schema:
        type: string
    Limit:
      name: limit
      in: query
      description: Number of items to return
      style: form
      explode: true
      schema:
        type: integer
        format: int32
        default: 20
        example: 20
        minimum: 1
        maximum: 100
    Offset:
      name: offset
      in: query
      description: Number of items to skip
      style: form
      explode: true
      schema:
        type: integer
        format: int32
        default: 0
        example: 20
    VideoSort:
      name: sort
      in: query
      description: >-
        Field to sort results by - if absent and there is a search string,
        results are sorted by relevance or if there is no search string, results
        are sorted by `-updated_at` - note: to sort in descending order, preface
        the field name with a minus(`-`) sign
      style: form
      explode: true
      schema:
        type: string
        enum:
          - name
          - reference_id
          - created_at
          - published_at
          - updated_at
          - schedule_starts_at
          - schedule_ends_at
          - state
          - plays_total
          - plays_trailing_week
        default: '-updated_at'
        example: created_at
        x-enum-elements:
          - name: name
            description: The video name
          - name: reference_id
            description: The video reference id
          - name: created_at
            description: Date video was created in the Video Cloud
          - name: published_at
            description: Date video was activated
          - name: updated_at
            description: Date video was last updated (by a user or the system)
          - name: schedule_starts_at
            description: Date video scheduled to become available
          - name: schedule_ends_at
            description: Date video scheduled to become unavailable
          - name: state
            description: Whether the video is playable (`ACTIVE` or `INACTIVE`)
          - name: plays_total
            description: >-
              All time video views (this field can only be sorted on - it is not
              returned)
          - name: plays_trailing_week
            description: >-
              Trailing week video views (this field can only be sorted on - it
              is not returned)
    PlaylistSort:
      name: sort
      in: query
      description: >-
        Field to sort results by - if absent and there is a search string,
        results are sorted by relevance or if there is no search string, results
        are sorted by `-updated_at` - note: to sort in descending order, preface
        the field name with a minus(`-`) sign
      style: form
      explode: true
      schema:
        type: string
        enum:
          - name
          - reference_id
          - created_at
          - published_at
          - updated_at
          - schedule_starts_at
          - schedule_ends_at
          - state
          - plays_total
          - plays_trailing_week
        default: '-updated_at'
        example: created_at
        x-enum-elements:
          - name: name
            description: The playlist name
          - name: updated_at
            description: Date playlist was last updated (by a user or the system)
    Search:
      name: q
      in: query
      description: >-
        Search string - see [search
        guide](https://support.brightcove.com/cmsplayback-api-videos-search) for
        details
      style: form
      explode: true
      schema:
        type: string
        example: '+tags:bird'
  schemas:
    AudioTrack:
      title: AudioTrack
      type: object
      properties:
        duration:
          type: integer
          description: URL the duration of the audio track in milliseconds
          readOnly: true
        encoding_rates:
          type: array
          items:
            type: integer
          description: array of encoding rates for the audio track renditions in KBPS
        id:
          type: string
          description: ID for the audio track formed as language_variant
          readOnly: true
        language:
          type: string
          description: language code for the audio track
          example: en
        variant:
          type: string
          description: >-
            the type of audio track (default can be set for the account by
            contacting Brightcove Support)
          enum:
            - main
            - alternate
            - commentary
            - dub
            - descriptive
    DigitalMaster:
      title: DigitalMaster
      type: object
      properties:
        encoding_rate:
          type: integer
          description: encoding rate in bps
          readOnly: true
        height:
          type: integer
          description: frame height in pixels
          readOnly: true
        width:
          type: integer
          description: frame width in pixels
          readOnly: true
        id:
          type: string
          description: the system id for the digital master
          readOnly: true
        size:
          type: integer
          description: file size in bytes
          readOnly: true
        updated_at:
          type: string
          description: date/time last modified
          readOnly: true
        duration:
          type: integer
          description: duration in milliseconds
    Drm:
      title: Drm
      type: object
      description: DRM packaging details (not applicable to remote assets)
      nullable: true
      readOnly: true
      properties:
        metadata_cdn_origin_id:
          type: string
        metadata_sharded_directory:
          type: string
        packager_version:
          type: string
        state:
          type: string
    Folder:
      title: Folder
      type: object
      properties:
        account_id:
          type: string
          description: Video Cloud account id
          readOnly: true
        created_at:
          type: string
          description: date/time folder created
          readOnly: true
        id:
          type: string
          description: system id for the folder
          readOnly: true
        name:
          type: string
          description: folder name
        updated_at:
          type: string
          description: date/time folder last modified
          readOnly: true
        video_count:
          type: string
          description: number of videos in the folder
          readOnly: true
    IngestJobStatus:
      title: Ingest Job Status
      type: object
      properties:
        account_id:
          type: string
          description: the Video Cloud account id
          readOnly: true
        error_code:
          type: string
          description: the error code if the job failed
          readOnly: true
        error_message:
          type: string
          description: the error message if the job failed
          readOnly: true
        id:
          type: string
          description: the ingest job id
          readOnly: true
        priority:
          type: string
          description: 'priority of the job (normal or low)'
          readOnly: true
        started_at:
          type: string
          description: when processing started
          readOnly: true
        state:
          type: string
          description: the current state of the ingest job
          readOnly: true
          enum:
            - processing
            - publishing
            - published
            - failed
          x-enum-elements:
            - name: processing
              description: >-
                video processing in progress - the video is not currently
                playable
            - name: publishing
              description: >-
                video processing in progress - at least one rendition is
                available, and the video is playable
            - name: published
              description: video processing is complete
            - name: failed
              description: video processing failed to complete
        submitted_at:
          type: string
          description: when the job was submitted
          readOnly: true
        updated_at:
          type: string
          description: when the status was last updated
          readOnly: true
        video_id:
          type: string
          description: the video id
          readOnly: true
    Playlist:
      title: Playlist
      type: object
      properties:
        account_id:
          type: string
          description: Video Cloud account id
          readOnly: true
        created_at:
          type: string
          description: date/time created
          format: date-time
          readOnly: true
        description:
          type: string
          description: playlist description
          maxLength: 255
        favorite:
          type: boolean
          description: whether playlist is in favorites list
          default: false
        id:
          type: string
          description: the playlist id
          readOnly: true
        name:
          type: string
          description: the playlist name
          maxLength: 255
        reference_id:
          type: string
          description: the playlist reference id
        search:
          type: string
          description: search string to retrieve the videos (smart playlists only)
        type:
          type: string
          description: 'the playlist type: EXPLICIT or smart playlist type'
          enum:
            - EXPLICIT
            - ACTIVATED_OLDEST_TO_NEWEST
            - ACTIVATED_NEWEST_TO_OLDEST
            - ALPHABETICAL
            - PLAYS_TOTAL
            - PLAYS_TRAILING_WEEK
            - START_DATE_OLDEST_TO_NEWEST
            - START_DATE_NEWEST_TO_OLDEST
        updated_at:
          type: string
          description: date/time last modified
          format: date-time
          readOnly: true
        video_ids:
          type: array
          items:
            type: string
          description: array of video ids (EXPLICIT playlists only)
    PlaylistReferences:
      title: PlaylistReferences
      required:
        - playlists
      type: object
      properties:
        playlists:
          type: array
          items:
            type: string
          description: array of EXPLICIT playlist ids that contain the video
    Subscription:
      title: Subscription
      type: object
      properties:
        endpoint:
          type: string
          description: the notifications endpoint
        events:
          type: array
          items:
            type: string
            enum:
              - video-change
          description: array of events subscribed to
        id:
          type: string
          description: system id for the subscription
          readOnly: true
        service_account:
          type: string
          description: the Video Cloud account id
          readOnly: true
    CreateVideoRequestBodyFields:
      title: Create Video Request Body Fields
      description: >-
        Only certain video metadata fields can be set when you initially create the video, and are listed here. Other fields must be set through an update (`PATCH`) request
        type: object
      properties:
        cue_points:
          description: Array of cuepoint objects
          items:
            $ref: '#/components/schemas/CuePoint'
          type: array
        custom_fields:
          description: >-
            map of `fieldname: value` pairs; values have a maximum length of 1024
              single-byte characters Note: be sure to use the **internal** name for
              the field, not the display name
          type: object
        description:
          description: Video short description
          type: string
        drm_disabled:
          description: >-
            Use to disable DRM packaging for this video - applies only to
            DRM-enabled accounts
          type: boolean
        economics:
          type: string
          description: whether the video supports ads
          enum:
            - AD_SUPPORTED
            - FREE
        geo:
          $ref: '#/components/schemas/Geo'
        long_description:
          description: A longer description of the video
          type: string
          maxLength: 5000
        name:
          description: Title of the video video
          type: string
          minLength: 1
          maxLength: 255
        reference_id:
          description: Reference id for the clip - must be unique within the account
          type: string
        schedule:
          $ref: '#/components/schemas/Schedule'
        state:
          type: string
          description: Whether the video should be active or inactive
          example: ACTIVE
          enum:
            - ACTIVE
            - INACTIVE
        tags:
          description: >-
            Array of tags for the video - note that  tags are string that may
            not contain a comma
          type: array
          items:
            type: string
      required:
        - name
    Video:
      title: Video
      type: object
      required:
        - name
      properties:
        ad_keys:
          type: string
          description: >-
            string representing the ad key/value pairs assigned to the video.
            Key/value pairs are formatted as key=value and are separated by
            ampersands - can only be added on update, not creation
          nullable: true
          example: ' "adKeys": "category=sports&live=true"'
        clip_source_video_id:
          type: string
          description: >-
            The ID of the source video that was clipped to produce this video or
            null if this video is not a clip of another video
          readOnly: true
        complete:
          type: boolean
          description: >-
            whether processing is complete &mdash; Note: when you create a new
            video, the complete property is automatically set to false. As soon
            as one rendition exists for the video, the complete property will be
            automatically set to true
          readOnly: true
        created_at:
          type: string
          description: when the video was created
          readOnly: true
          example: '2015-09-17T16:08:37.108Z'
        cue_points:
          type: array
          items:
            $ref: '#/components/schemas/CuePoint'
          description: 'array of cue points - can only be added on update, not creation'
        custom_fields:
          type: object
          additionalProperties:
            type: string
            description: >-
              map of fieldname-value pairs; values have a maximum length of 1024
              single-byte characters Note: be sure to use the internal name for
              the field, not the display name
        delivery_type:
          type: string
          description: video delivery type
          enum:
            - remote
            - static_origin
            - dynamic_origin
            - live_origin
            - unknown
          readOnly: true
        description:
          type: string
          description: video short description
          maxLength: 248
        digital_master_id:
          type: string
          description: asset id of the digital master
          readOnly: true
        drm_disabled:
          type: boolean
          description: >-
            if true, the video is not DRM-packaged - applies to accounts that
            are enabled for DRM only
        duration:
          type: integer
          description: video duration in milliseconds
          readOnly: true
        economics:
          type: string
          description: whether video is `AD_SUPPORTED`
          default: AD_SUPPORTED
          enum:
            - AD_SUPPORTED
            - FREE
        folder_id:
          type: string
          description: id of the folder that contains the video
          readOnly: true
        geo:
          $ref: '#/components/schemas/Geo'
        has_digital_master:
          type: boolean
          description: >-
            whether video has an archived master than can be used for
            retranscoding
          readOnly: true
        id:
          type: string
          description: video id
          readOnly: true
        images:
          $ref: '#/components/schemas/VideoImages'
        link:
          $ref: '#/components/schemas/Link'
        live:
          $ref: '#/components/schemas/Live'
        long_description:
          type: string
          description: 'video long description - can only be added on update, not creation'
          maxLength: 5000
        name:
          type: string
          description: video title
          minLength: 1
          maxLength: 255
        offline_enabled:
          type: boolean
          description: whether video is enabled for offline viewing
          default: false
        original_filename:
          type: string
          description: the original file name for the uploaded video
          readOnly: true
        projection:
          type: string
          description: used for 360 videos
          nullable: true
          enum:
            - equirectangular
        published_at:
          type: string
          description: >-
            start date-time of first activation in
            ISO-8601(http://www.ecma-international.org/ecma-262/5.1/#sec-15.9.1.15)
            format
          readOnly: true
        reference_id:
          type: string
          description: video reference-id (must be unique within the account)
          nullable: true
          maxLength: 150
        schedule:
          $ref: '#/components/schemas/Schedule'
        sharing:
          $ref: '#/components/schemas/Sharing'
          readOnly: true
        state:
          type: string
          description: state determines whether the video is playable or not
          enum:
            - ACTIVE
            - INACTIVE
        tags:
          type: array
          items:
            type: string
          description: array of tags
        text_tracks:
          type: array
          items:
            $ref: '#/components/schemas/TextTrack'
          description: 'array of text tracks - can only be added on update, not creation'
        updated_at:
          type: string
          description: when the video was last modified
          readOnly: true
    Live:
      title: live
      description: Properties for a live stream created by the Live Module or Live API
      type: object
      properties:
        type:
          type: string
          description: the live stream type
          readOnly: true
          enum:
            - EVENT
        job_id:
          type: string
          description: the live job id
          readOnly: true
        job_state:
          type: string
          description: the current state of the job
          readOnly: true
          enum:
            - error
            - standby
            - waiting
            - processing
            - disconnected
            - cancelling
            - finishing
            - cancelled
            - finished
            - failed
          ended_at:
            type: string
            description: the date-time that the live stream ended
            readOnly: true
          name:
            type: string
            description: the name of the live job
            readOnly: true
          clip_started_at:
            type: string
            description: the starting point for a VOD clip
            readOnly: true
          clip_ended_at:
            type: string
            description: the ending point for a VOD clip
            readOnly: true
          updated_at:
            type: string
            description: the date-time the live job was last updated
            readOnly: true
    CuePoint:
      title: cue_point
      required:
        - time
        - type
      type: object
      properties:
        id:
          type: string
          description: System id for the cuepoint
          readOnly: true
        time:
          type: number
          description: time of the cue point in seconds
          example: 10.527
        type:
          type: string
          description: cue point type
          enum:
            - AD
            - DATA
        force-stop:
          type: boolean
          description: >-
            whether video is force-stopped at the cue point (currently igmnored
            by the Brightcove Player)
          default: false
        metadata:
          type: string
          description: optional metadata string (512 single-byte characters maximum)
          maxLength: 512
        name:
          type: string
          description: cue point name
    Geo:
      title: geo
      description: map of geo-filtering properties
      required:
        - countries
        - exclude_countries
        - restricted
      type: object
      properties:
        countries:
          type: string
          description: >-
            array of ISO 3166 list of 2-letter
            codes(https://www.iso.org/obp/ui/#home) (search for "country codes")
        exclude_countries:
          type: boolean
          description: >-
            if true, country array is treated as a list of countries excluded
            from viewing
        restricted:
          type: boolean
          description: whether geo-restriction is enabled for this video
    VideoImages:
      title: VideoImages
      description: map of image maps
      readOnly: true
      type: object
      properties:
        poster:
          $ref: '#/components/schemas/Image'
        thumbnail:
          $ref: '#/components/schemas/Image'
    Image:
      title: image
      description: map of poster or thumbnail properties
      required:
        - sources
        - src
      type: object
      properties:
        sources:
          type: array
          items:
            $ref: '#/components/schemas/Image.sources'
          description: array of image source maps
        src:
          type: string
          description: URL for the default image source image
          readOnly: true
        asset_id:
          type: string
          description: asset id for the image
          readOnly: true
    Image.sources:
      title: image.sources
      required:
        - src
      type: object
      properties:
        src:
          type: string
          description: URL for a image source image
    Link:
      title: link
      description: >-
        map of related link properties - can only be added on update, not
        creation
      type: object
      properties:
        text:
          type: string
          description: text for the link
          maxLength: 255
        url:
          type: string
          description: URL for the link
          maxLength: 255
    Schedule:
      title: schedule
      description: map of scheduling properties
      type: object
      properties:
        ends_at:
          type: string
          description: >-
            end date-time of availability in
            ISO-8601(http://www.ecma-international.org/ecma-262/5.1/#sec-15.9.1.15)
            format
          nullable: true
        starts_at:
          type: string
          description: >-
            start date-time of availability in
            ISO-8601(http://www.ecma-international.org/ecma-262/5.1/#sec-15.9.1.15)
            format
          nullable: true
    Sharing:
      title: sharing
      description: map of the sharing properties for the video
      type: object
      properties:
        by_external_acct:
          type: boolean
          description: whether the video was shared from another account
          readOnly: true
        by_id:
          type: string
          description: >-
            id of the account that shared the video; note that this field is
            populated only for the shared copy, not for the original video
          readOnly: true
        by_reference:
          type: boolean
          description: whether the video is shared by reference
          readOnly: true
        source_id:
          type: string
          description: >-
            id of the video in its original account; note that this field is
            populated only for the shared copy, not for the original video
          readOnly: true
        to_external_acct:
          type: boolean
          description: whether the video is shared to another account
          readOnly: true
    TextTrack:
      title: text_track
      required:
        - src
        - srclang
      type: object
      properties:
        id:
          type: string
          description: System id for the text track
          readOnly: true
        src:
          type: string
          description: URL for the .vtt file
        srclang:
          type: string
          description: '2-letter language code, such as "en" or "ko"'
        default:
          type: boolean
          description: >-
            whether this is the default track - should only be true for one text
            track
          default: false
        kind:
          type: string
          description: kind of text track
          default: captions
          enum:
            - subtitles
            - captions
            - descriptions
            - chapters
            - metadata
        label:
          type: string
          description: label for the track
        mime_type:
          type: string
          description: mime-type for the track
    VideoFields:
      title: VideoFields
      type: object
      properties:
        custom_fields:
          type: array
          items:
            $ref: '#/components/schemas/Videofields.customField'
          description: array of standard field maps
        max_custom_fields:
          type: string
          description: maximum number of custom fields for the account
        standard_fields:
          type: array
          items:
            $ref: '#/components/schemas/Videofields.standardField'
          description: array of standard field maps
    Videofields.customField:
      title: VideoFields.custom_field
      type: object
      properties:
        description:
          type: string
          description: description (instruction for user)
        display_name:
          type: string
          description: display name
        enum_values:
          type: array
          items:
            type: string
          description: array of string values for select type fields
        id:
          type: string
          description: 'data name for the field (used to access it in searches, etc.)'
        required:
          type: boolean
          description: whether field must have a value before video can be active
        type:
          type: string
          description: custom field type (enum or string)
    Videofields.standardField:
      title: VideoFields.standard_field
      type: object
      properties:
        description:
          type: string
          description: description (instruction for user)
        id:
          type: string
          description: 'data name for the field (used to access it in searches, etc.)'
        required:
          type: boolean
          description: whether field must have a value before video can be active
    VideoSources:
      title: VideoSources
      type: object
      properties:
        app_name:
          type: string
          description: address for RTMP stream
          readOnly: true
        codec:
          type: string
          description: the video codec for the rendition
        container:
          type: string
          description: the video container for the rendition
        duration:
          type: integer
          description: duration in milliseconds
        encoding_rate:
          type: integer
          description: encoding rate in bps
        height:
          type: integer
          description: frame height in pixels
          readOnly: true
        remote:
          type: boolean
          description: whether the source is a remote asset
        size:
          type: integer
          description: file size in bytes
        src:
          type: string
          description: URL for HTTP rendition
          readOnly: true
        stream_name:
          type: string
          description: the stream name on the CDN
          readOnly: true
        type:
          type: string
          description: the type for segmented streams
          readOnly: true
        width:
          type: integer
          description: frame width in pixels
          readOnly: true
        asset_id:
          type: string
          description: system id for the rendition
          readOnly: true
        uploaded_at:
          type: string
          description: date/time when the video was uploaded
          readOnly: true
    ShareVideoRequest:
      title: ShareVideoRequest
      required:
        - id
      type: object
      properties:
        id:
          type: string
          description: affiliate account id to share to
    VideoShare:
      title: VideoShare
      required:
        - video_id
        - affiliate_id
        - affiliate_video_id
      type: object
      properties:
        video_id:
          type: string
          description: the video id
        affiliate_id:
          type: string
          description: the id of the affiliate account the video is shared to
        affiliate_video_id:
          type: string
          description: the id of the shared video in the affiliate account
        statue:
          type: string
          description: 'the status of the sharing - either PROCESSING, COMPLETE, or FAILED'
        shared_at:
          type: string
          description: when the video was shared
        updated_at:
          type: string
          description: the video was last updated in the affiliate account
    AddAffiliate:
      title: AddAffiliate
      type: object
      properties:
        account_id:
          type: string
          description: affiliate account id
    Channel:
      title: Channel
      description: the channel this contract belongs to
      type: object
      properties:
        account_id:
          type: string
          description: master account id
        name:
          type: string
          description: channel name
        enforce_custom_fields:
          type: boolean
          description: >-
            if true, will allow sharing only if the affiliate account has all
            custom fields that have values for the video to be shared
        enforce_geo:
          type: boolean
          description: 'if true, and master account is enabled for geo-filtering'
        account_name:
          type: string
          description: master account name
        created_at:
          type: string
          description: when the channel was created
          readOnly: true
        updated_at:
          type: string
          description: when the channel was last updated
    Contract:
      title: Contract
      type: object
      properties:
        account_id:
          type: string
          description: affiliate account id
        approved:
          type: boolean
          description: whether the contract is approved
        auto_accept:
          type: boolean
          description: whether shared videos will be automatically accepted
        approved_at:
          type: string
          description: when the contract was approved
        created_at:
          type: string
          description: when the contract was created
          readOnly: true
        updated_at:
          type: string
          description: when the contract was last updated
        channel:
          $ref: '#/components/schemas/Channel'
    ChannelAffiliate:
      title: ChannelAffiliate
      required:
        - account_id
        - account_name
        - approved
      type: object
      properties:
        account_id:
          type: string
          description: affiliate account id
        account_name:
          type: string
          description: affiliate account name
        approved:
          type: boolean
          description: whether the affiliate has approved the relationship
    VideoAsset:
      title: VideoAsset
      required:
        - controller_type
        - current_filename
        - encoding_rate
        - frame_height
        - frame_width
        - key_systems
        - name
        - progressive_download
        - reference_id
        - remote_url
        - remote_stream_name
        - size
        - type
        - video_codec
        - video_container
        - video_duration
      type: object
      properties:
        controller_type:
          type: string
          description: >-
            the controller type for ingested renditions (not applicable to
            remote renditions or other types of assets)
        current_filename:
          type: string
          description: >-
            the filename for an ingested asset in the Video Cloud system (not
            applicable to remote assets)
        drm:
          $ref: '#/components/schemas/Drm'
        encoding_rate:
          type: integer
          description: average encoding rate in kbps
        frame_height:
          type: integer
          description: frame height in pixels
        frame_width:
          type: integer
          description: frame width in pixels
        key_systems:
          type: array
          items:
            type: string
          description: >-
            array of strings that denote the kind of encryption used for DRM
            packaged renditions
        name:
          type: string
          description: asset name
        progressive_download:
          type: boolean
          description: >-
            whether ingested rendition is available by progressive download (not
            applicable to other asset types or remote renditions)
        reference_id:
          type: string
          description: video reference id (must be unique within the account)
        remote_url:
          type: string
          description: the url for a remote asset (not applicable to ingested assets)
          maxLength: 250
        remote_stream_name:
          type: string
          description: >-
            name for remote streams (not applicable to asset types other than
            rendition)
        size:
          type: integer
          description: the size of the asset in bytes (integer)
        type:
          type: string
          description: the type of the asset
        video_codec:
          type: string
          description: not applicable to remote assets
        video_container:
          type: string
          description: not applicable to remote assets
        video_duration:
          type: integer
          description: duration in milliseconds
        account_id:
          type: string
          description: the Video Cloud account id
        id:
          type: string
          description: the asset id
          readOnly: true
        audio_only:
          type: boolean
          description: >-
            this rendition contains only an audio track, no video track (a
            rendition for low bandwidth devices)
        cdn_origin_id:
          type: string
          description: an internally used id (not applicable to remote assets)
        complete:
          type: boolean
          description: >-
            whether processing is complete for the asset (will be true for
            remote assets if a remote_url is supplied)
        updated_at:
          type: string
          description: when the video was last modified
        uploaded_at:
          type: string
          description: when the asset was added to the video in Video Cloud
    Manifest:
      title: Manifest
      required:
        - audio_only
        - controller_type
        - current_filename
        - name
        - progressive_download
        - reference_id
        - remote_url
        - remote_stream_name
        - size
        - type
        - video_duration
      type: object
      properties:
        audio_only:
          type: boolean
          description: not applicable to ism_manifests
        controller_type:
          type: string
          description: >-
            the controller type for ingested renditions (not applicable to
            remote renditions or other types of assets)
        current_filename:
          type: string
          description: >-
            the filename for an ingested asset in the Video Cloud system (not
            applicable to remote assets)
        name:
          type: string
          description: asset name
        progressive_download:
          type: boolean
          description: >-
            whether ingested rendition is available by progressive download (not
            applicable to other asset types or remote renditions)
        reference_id:
          type: string
          description: video reference id (must be unique within the account)
        remote_url:
          type: string
          description: the url for a remote asset (not applicable to ingested assets)
          maxLength: 250
        remote_stream_name:
          type: string
          description: >-
            name for remote streams (not applicable to asset types other than
            rendition)
        size:
          type: integer
          description: the size of the asset in bytes (integer)
        type:
          type: string
          description: the type of the asset
        video_duration:
          type: integer
          description: >-
            video duration in seconds (present if you included it when you
            created the asset)
        id:
          type: string
          description: the asset id
          readOnly: true
        complete:
          type: boolean
          description: >-
            whether processing is complete for the asset (will be true for
            remote assets if a remote_url is supplied)
        cdn_origin_id:
          type: string
          description: an internally used id (not applicable to remote assets)
        updated_at:
          type: string
          description: when the video was last modified
        uploaded_at:
          type: string
          description: when the asset was added to the video in Video Cloud
    ImageAsset:
      title: ImageAsset
      required:
        - audio_only
        - controller_type
        - current_filename
        - name
        - progressive_download
        - reference_id
        - remote_url
        - remote_stream_name
        - size
        - type
      type: object
      properties:
        audio_only:
          type: boolean
          description: not applicable to posters
        controller_type:
          type: string
          description: >-
            the controller type for ingested renditions (not applicable to
            remote renditions or other types of assets)
        current_filename:
          type: string
          description: >-
            the filename for an ingested asset in the Video Cloud system (not
            applicable to remote assets)
        name:
          type: string
          description: asset name
        progressive_download:
          type: boolean
          description: >-
            whether ingested rendition is available by progressive download (not
            applicable to other asset types or remote renditions)
        reference_id:
          type: string
          description: video reference id (must be unique within the account)
        remote_url:
          type: string
          description: the url for a remote asset (not applicable to ingested assets)
          maxLength: 250
        remote_stream_name:
          type: string
          description: >-
            name for remote streams (not applicable to asset types other than
            rendition)
        size:
          type: integer
          description: the size of the asset in bytes (integer)
        type:
          type: string
          description: the type of the asset
        id:
          type: string
          description: the asset id
          readOnly: true
        complete:
          type: boolean
          description: >-
            whether processing is complete for the asset (will be true for
            remote assets if a remote_url is supplied)
        cdn_origin_id:
          type: string
          description: an internally used id (not applicable to remote assets)
        updated_at:
          type: string
          description: when the video was last modified
        uploaded_at:
          type: string
          description: when the asset was added to the video in Video Cloud
    DynamicRendition:
      title: DynamicRendition
      required:
        - rendition_id
        - media_type
        - encoding_rate
        - frame_height
        - frame_width
        - size
        - updated_at
        - uploaded_at
        - duration
        - audio_configuration
        - variant
      type: object
      properties:
        rendition_id:
          type: string
          description: the rendition id
          readOnly: true
        media_type:
          type: boolean
          description: media type for the rendition (audio or video)
          readOnly: true
          enum:
            - audio
            - video
        encoding_rate:
          type: integer
          description: average encoding rate in kbps
          readOnly: true
        frame_height:
          type: integer
          description: frame height in pixels
          readOnly: true
        frame_width:
          type: integer
          description: frame width in pixels
          readOnly: true
        size:
          type: integer
          description: the size of the asset in bytes (integer)
          readOnly: true
        updated_at:
          type: string
          description: when the video was last modified
          readOnly: true
        uploaded_at:
          type: string
          description: when the asset was added to the video in Video Cloud
          readOnly: true
        duration:
          type: integer
          description: duration in milliseconds
          readOnly: true
        audio_configuration:
          type: string
          description: The audio configuration of the audio track
          readOnly: true
        variant:
          type: string
          description: 'The variant of the HLS profile (baseline, main, or high)'
          readOnly: true
          enum:
            - baseline
            - main
            - high
    PlaylistCount:
      title: Get_Playlist_CountResponse
      type: object
      properties:
        count:
          type: integer
          description: count of videos
    VideoCountInPlaylist:
      title: Get_Video_Count_in_PlaylistResponse
      type: object
      properties:
        count:
          type: integer
          description: count of videos
    VideoCount:
      title: Get_Video_CountResponse
      type: object
      properties:
        count:
          type: integer
          description: count of videos
  securitySchemes:
    BC_OAuth2:
      type: oauth2
      description: >-
        Brightcove OAuth API. See the [support
        documentation](https://support.brightcove.com/oauth-api) or [Getting
        Access
        Tokens](https://support.brightcove.com/oauth-api-sample-get-access-token)
        to learn more
      flows:
        clientCredentials:
          tokenUrl: 'https://oauth.brightcove.com/v4/access_token'
          scopes:
            video-cloud/video/read: Video Read
            video-cloud/video/create: Video Create
            video-cloud/video/update: Video Update
            video-cloud/video/delete: Video Delete
            video-cloud/video/all: Video ALL
            video-cloud/asset/delete: Digital Master Delete
            video-cloud/playlist/read: Playlist Read
            video-cloud/playlist/create: Playlist Create
            video-cloud/playlist/update: Playlist Update
            video-cloud/playlist/delete: Playlist Delete
            video-cloud/playlist/all: Playlist All
            video-cloud/sharing-relationships/read: Sharing Relationships Read
            video-cloud/sharing-relationships/create: Sharing Relationships Create
            video-cloud/sharing-relationships/update: Sharing Relationships Update
            video-cloud/sharing-relationships/delete: Sharing Relationships Delete
            video-cloud/sharing-relationships/all: Sharing Relationships All
            video-cloud/notifications/all: Notifications All
